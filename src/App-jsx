import React, { useState } from "react";
import Login from "./pages/login";
import Home from "./pages/home";

export default function App() {
  const [logado, setLogado] = useState(false);

  return (
    <>
      {logado ? <Home /> : <Login onLogin={() => setLogado(true)} />}
    </>
  );
}
